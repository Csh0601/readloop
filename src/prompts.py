"""ReadLoop Analysis Prompts -- 深度论文分析 + 跨论文 Harness"""

PAPER_ANALYSIS = """你是一位在 NeurIPS/ICML 发表过多篇论文的资深 AI 研究员，同时也是系统架构师。
对以下论文进行 **深度拆解**，达到"读完你的分析等于精读了原文"的水平。

论文全文:
<paper>
{paper_text}
</paper>

按以下结构输出 Markdown 报告。每一节都必须有实质内容，不允许一两句话带过。

---

# {paper_title}

## 1. 论文元数据

| 字段 | 内容 |
|------|------|
| **原标题** | |
| **作者与机构** | |
| **发表渠道** | (会议/期刊/ArXiv, 年份) |
| **领域标签** | (3-5个) |

## 2. 一句话定位

> 一句话概括核心贡献，体现: 解决什么问题 + 用什么方法 + 达到什么效果。不要写"本文提出了一种方法"这种废话。

## 3. 问题剖析

### 3.1 核心问题
（用研究者视角精确定义问题本质。不是复述 abstract，而是: 这个问题的本质是什么？为什么难？）

### 3.2 现有方法的具体缺陷
（逐一指出: 具体哪个方法 → 在什么场景下 → 因为什么原因失败/不足）

### 3.3 问题重要性
（解决它对领域和工业界的具体影响）

## 4. 核心创新点深度分析

> 每个创新点 200-400 字深度分析。

### 创新点 1: [名称]

**创新本质**: (一句话)

**技术细节**: (具体做了什么 / 算法如何运作 / 关键设计选择)

**对比分析**:
| 维度 | 之前做法 | 本文做法 | 改进原因 |
|------|---------|---------|---------|
| | | | |
| | | | |

**创新性评估**: (渐进式改进 or 范式转变？)

### 创新点 2: [名称]
（同上格式，必须有对比表格）

### 创新点 3: [名称]
（如果有）

## 5. 系统架构全景

### 5.1 架构概述
（3-5 段，描述从输入到输出的全流程，包含数据流、控制流、关键决策点）

### 5.2 系统架构图

```mermaid
graph TD
    subgraph 输入层
        A[具体输入描述]
    end
    subgraph 核心处理
        B[模块1<br/>具体功能] -->|传递什么| C[模块2<br/>具体功能]
        C -->|传递什么| D[模块3<br/>具体功能]
    end
    subgraph 输出层
        D -->|最终产物| E[具体输出]
    end
```

> 【要求】: 反映论文实际架构；节点标注具体功能；边标注数据类型；用 subgraph 分层；多条路径要分别画出

### 5.3 数据流图

```mermaid
flowchart LR
    Raw[原始输入] --> Process1[处理步骤1] --> Process2[处理步骤2] --> Output[输出]
```

> 展示数据在系统中如何被逐步变换

### 5.4 模块详解
（每个关键模块: 输入/处理/输出/设计动机）

## 6. 技术实现细节

### 6.1 核心算法步骤
（结构化步骤描述核心算法）

### 6.2 关键机制解析
（逐一说明每个重要技术机制: 是什么 / 为什么需要 / 如何工作）

### 6.3 设计选择矩阵
| 设计选择 | 论文选择 | 替代方案 | 选择理由 |
|---------|---------|---------|---------|
| | | | |

## 7. 实验深度分析

### 7.1 实验设置
| 项目 | 详情 |
|------|------|
| 数据集 | |
| 基准方法 | |
| 评估指标 | |
| 计算资源 | |

### 7.2 主要结果
（表格呈现关键数值，标注最优值）

### 7.3 关键发现
（解读数字: 什么场景表现最好/最差？为什么？）

### 7.4 消融实验
（各组件贡献多大？有无意外发现？）

## 8. 工程化实现蓝图

### 8.1 项目架构

```mermaid
graph TD
    A[project root] --> B[core/]
    A --> C[models/]
    A --> D[data/]
    A --> E[eval/]
    A --> F[config/]
    B --> B1[核心算法模块]
    C --> C1[模型定义]
    D --> D1[数据处理管道]
```

> 如果要复现这篇论文，最合理的项目结构

### 8.2 关键实现要点
（工程化最关键的 3-5 个要点和潜在的坑）

### 8.3 技术栈建议
| 组件 | 推荐 | 理由 |
|------|------|------|
| | | |

## 9. 批判性评估

### 9.1 核心优势 (4+ 点，每点 2-3 句)
### 9.2 关键局限 (3+ 点，要具体)
### 9.3 论文存疑之处
（实验设计不严谨 / 结论过于乐观 / 对比不公平的地方？）

## 10. 研究版图定位

```mermaid
graph LR
    P1[前置工作1] -->|启发| This[本文]
    P2[前置工作2] -->|改进| This
    This -->|开启| F1[后续方向1]
    This -->|推动| F2[后续方向2]
```

- 继承了哪些工作？超越了什么？开启了什么新方向？

## 11. 对 Agent Memory 项目的可迁移价值

### 11.1 可直接借鉴的技术
（具体到哪个模块/算法可拿来用，用在什么场景）

### 11.2 架构设计启发
（论文的哪些架构理念可指导我们的系统设计）

### 11.3 需要规避的问题
（论文暴露的局限性，我们如何规避）

## 弹性原则

以上 11 节是推荐结构，请根据论文实际情况灵活调整：
- Survey/综述：可省略第5节（系统架构）和第7.4节（消融实验），扩展第3节和第10节
- Benchmark/评测：可省略第4节（创新点深度分析），扩展第7节
- 某一节确实不适用时，整节省略比硬填更好
- Mermaid 图只在确实有助于理解时绘制，不强制
- 重点是分析的深度和准确性，而非格式完整性

---
"""

CROSS_ANALYSIS = """你是 Agent Memory 领域深耕多年的研究员和系统架构师。
你正在为一个 **Agent Harness 驱动的论文分析系统** 生成核心输出。

这份跨论文综合分析是整个 Harness 的 **核心卖点**:
它要展示出单独读任何一篇论文都无法获得的洞察——
通过结构化提取 + 自动交叉分析，发现论文间隐藏的关联、矛盾和互补。

以下是 {count} 篇论文的分析摘要:

{papers_summaries}

---

# Agent Memory 研究全景分析报告

> 由 ReadLoop Agent Harness 自动生成。
> 综合 {count} 篇论文的系统化分析，提炼超越单篇的研究洞察。

## 1. 研究版图

### 1.1 论文分类矩阵

| 类别 | 论文 | 核心方法 | 解决的关键问题 |
|------|------|---------|---------------|
| | | | |

### 1.2 研究趋势时间线

```mermaid
timeline
    title Agent Memory 研究演进
    section 早期探索
        关键论文 : 核心贡献
    section 方法成熟
        关键论文 : 核心贡献
    section 前沿突破
        关键论文 : 核心贡献
```

### 1.3 热度分布分析
（哪些子方向最密集？哪些刚起步？反映什么趋势？）

## 2. 核心技术路线对比

### 2.1 方法论全维度对比

| 维度 | 方法A | 方法B | 方法C |
|------|------|------|------|
| 核心思路 | | | |
| 记忆组织 | | | |
| 检索策略 | | | |
| 压缩机制 | | | |
| 多Agent支持 | | | |
| 可扩展性 | | | |
| 实验验证 | | | |

### 2.2 技术路线演进图

```mermaid
graph TD
    subgraph 记忆组织
        A[向量存储] --> B[结构化图谱] --> C[混合存储]
    end
    subgraph 上下文管理
        D[固定窗口] --> E[动态压缩] --> F[自适应选择]
    end
    subgraph 多Agent协作
        G[共享状态] --> H[消息传递] --> I[分布式记忆]
    end
    C & F & I --> J[下一代统一架构]
```

> 节点必须关联到具体论文

### 2.3 创新点对比矩阵

| 创新点 | 提出论文 | 技术本质 | 适用场景 | 局限 |
|--------|---------|---------|---------|------|
| | | | | |

## 3. 知识关联图谱

```mermaid
graph LR
    subgraph 记忆架构
        A[论文A] -->|改进| B[论文B]
    end
    subgraph 上下文优化
        C[论文C] -->|对比| D[论文D]
    end
    A -.->|跨域启发| C
```

> 用论文简称; 边标注关系类型(改进/对比/互补/启发/评估); subgraph 按主题分组; 虚线表示跨域关联

## 4. 共性问题与分歧解法

### 4.1 核心挑战对比

| 挑战 | 本质原因 | 论文A解法 | 论文B解法 | 最优解分析 |
|------|---------|----------|----------|----------|
| | | | | |

### 4.2 尚未达成共识的争议
（不同论文给出矛盾结论的地方？矛盾背后原因？）

### 4.3 被所有论文忽略的盲区

## 5. 统一系统架构蓝图

> 综合所有论文最佳实践，设计理想的 Agent Memory 系统

```mermaid
graph TD
    subgraph 感知层
        A[多模态输入] --> B[理解与编码]
    end
    subgraph 记忆层
        C[工作记忆<br/>短期高速]
        D[情景记忆<br/>中期检索]
        E[语义记忆<br/>长期结构化]
        B --> C --> D --> E
    end
    subgraph 管理层
        F[写入控制] & G[检索引擎] & H[遗忘压缩] & I[跨Agent同步]
    end
    subgraph 应用层
        J[单Agent推理] & K[多Agent协作] & L[长期任务]
    end
    C & D & E --> F & G & H & I --> J & K & L
```

> 每个模块标注灵感来源（哪篇论文）

### 5.1 各模块最佳实践
（每个模块采用哪篇论文的方案？为什么？）

### 5.2 实现路线图

```mermaid
gantt
    title 实现路线
    dateFormat YYYY-MM
    section 基础层
    记忆存储 :a1, 2026-05, 2w
    检索引擎 :a2, after a1, 2w
    section 进阶
    压缩机制 :b1, after a2, 2w
    多Agent同步 :b2, after b1, 3w
    section 验证
    基准测试 :c1, after b2, 2w
```

## 6. 研究空白与机会

| 方向 | 为什么重要 | 为什么没人做 | 切入点 |
|------|-----------|------------|--------|
| | | | |

## 7. Harness 价值展示

> 这一节展示 Agent Harness 的独特价值

### 7.1 本次分析发现的跨论文洞察
（列出 5+ 个单独读论文不容易发现的洞察）

### 7.2 Harness 能力总结
1. **批量摄入**: 短时间处理大量论文
2. **结构化提取**: 统一维度信息提取
3. **交叉分析**: 自动发现关联、矛盾、互补
4. **知识沉淀**: 分散知识凝聚为可检索图谱
5. **架构综合**: 从多篇论文中提炼最优架构蓝图

## 8. 推荐阅读路径

```mermaid
graph TD
    A[入门] --> B[进阶]
    B --> C[前沿]
    C --> D[工程实践]
```

- **快速了解领域**: ...
- **做 Agent Memory 系统**: ...
- **写综述论文**: ...

---
*ReadLoop Agent Harness | {count} papers analyzed*
"""


EXTRACT_PAPER_DIGEST = """从以下论文分析报告中提取结构化精华，用JSON格式返回。

{analysis_text}

返回格式（严格 JSON，不要 markdown fence）：
{{
  "title": "论文标题",
  "one_liner": "一句话总结（问题+方法+结果）",
  "method": "核心方法描述（100字以内）",
  "key_claims": ["主要发现1", "主要发现2", "主要发现3"],
  "evaluation": "实验评估总结（100字以内，含关键指标数据）",
  "limitations": ["局限1", "局限2"],
  "domain_tags": ["tag1", "tag2", "tag3"]
}}
"""


CROSS_ANALYSIS_STAGE_A = """你是 Agent Memory 领域的跨论文分析专家。以下是 {count} 篇论文的结构化精华：

{digests}

请从中发现跨论文的洞察（单独读论文不容易发现的）。返回 JSON：
{{
  "insights": [
    {{
      "claim": "具体洞察（一句话，要具体到方法名/指标/结论）",
      "evidence_papers": ["论文1标题", "论文2标题"],
      "type": "comparison|gap|pattern|contradiction"
    }}
  ]
}}

要求：
- 每条 insight 必须引用至少 2 篇论文
- 不要泛泛而谈，要具体到方法名、指标、结论
- 生成 10-20 条候选 insights
- type 含义：comparison=方法对比, gap=研究空白, pattern=共性趋势, contradiction=矛盾发现
"""


CROSS_ANALYSIS_VERIFY = """验证以下跨论文洞察是否有证据支撑。

Claim: {claim}

Evidence from cited papers:
{evidence}

返回 JSON：
{{
  "grounded": true/false,
  "reason": "简要说明为什么有/没有证据支撑（1-2句）"
}}

严格判断标准：
- claim 中的具体结论/对比/数据必须能在 evidence 中找到依据
- 仅仅"主题相关"不算 grounded
- 如果 claim 做了 evidence 中没有的推断，判 false
"""


CROSS_ANALYSIS_FINAL = """你是 Agent Memory 领域深耕多年的研究员和系统架构师。
你正在为一个 Agent Harness 驱动的论文分析系统生成核心输出。

基于 {count} 篇论文的精华和已验证的跨论文洞察，生成综合分析报告。

## 论文精华

{digests}

## 已验证的洞察（每条都有证据支撑）

{verified_insights}

请生成完整的跨论文综合分析报告，包含以下部分：

1. **研究版图**: 论文分类矩阵、研究热点分布
2. **核心技术路线对比**: 方法对比表（至少 7 个维度）、创新矩阵
3. **知识关联图谱**: 论文间的关系（改进/对比/互补/启发）
4. **共性问题与分歧解法**: 核心挑战对比、未解决的分歧、所有论文都忽视的盲区
5. **统一系统架构蓝图**: 综合最优实践的架构设计
6. **研究空白与机会**: 方向、重要性、切入点
7. **Harness 价值展示**: 5+ 条跨论文洞察（必须基于上方已验证的洞察）
8. **推荐阅读路径**: 入门→进阶→前沿→工程实践

重要：第 7 节的洞察必须引用具体论文名和证据，不要凭空编造。

---
*ReadLoop Agent Harness | {count} papers analyzed*
"""
